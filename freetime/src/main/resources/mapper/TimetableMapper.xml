<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.TimetableMapper">
	<select id="list" resultType="hashmap">
		select * from timetable
	</select>
	<select id="read" resultType="hashmap">
		select * from timetable where uid=#{uid}
	</select>
	<update id="update">
		update timetable
		set gid=#{gid}, gscore=#{gscore}, gcredit=#{gcredit}, ggrade=#{ggrade}
		where uid=#{uid}
	</update>
	<delete id="delete">
		delete from timetable where uid=#{uid}
	</delete>
	<insert id="insert">
	  	insert into timetable (uid, gid, gscore, gcredit, ggrade)
	  	values (#{uid}, #{gid}, #{gscore}, #{gcredit}, #{ggrade})
	</insert>
</mapper>